<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/feedFragment">

    <fragment
        android:id="@+id/feedFragment"
        android:name="ru.maxim.barybians.ui.fragment.feed.FeedFragment"
        android:label="FeedFragment"
        tools:layout="@layout/fragment_feed" />

    <fragment
        android:id="@+id/dialogsListFragment"
        android:name="ru.maxim.barybians.ui.fragment.dialogsList.DialogsListFragment"
        android:label="DialogsListFragment"
        tools:layout="@layout/fragment_dialogs_list">

    </fragment>

    <fragment
        android:id="@+id/profileFragment"
        android:name="ru.maxim.barybians.ui.fragment.profile.ProfileFragment"
        android:label="ProfileFragment"
        tools:layout="@layout/fragment_profile">

        <argument
            android:name="userId"
            app:argType="integer" />

        <action
            android:id="@+id/profileToSettings"
            app:destination="@id/settingsFragment"
            app:launchSingleTop="false"
            app:popUpTo="@id/profileFragment" />
    </fragment>

    <fragment
        android:id="@+id/settingsFragment"
        android:name="ru.maxim.barybians.ui.activity.preferences.PreferencesFragment"
        android:label="SettingsFragment"
        tools:layout="@xml/fragment_preferences" />

    <fragment
        android:id="@+id/chatFragment"
        android:name="ru.maxim.barybians.ui.fragment.chat.ChatFragment"
        android:label="ChatFragment"
        tools:layout="@layout/fragment_chat">

        <argument
            android:name="userId"
            app:argType="integer"/>
    </fragment>

    <action
        android:id="@+id/toProfile"
        app:destination="@id/profileFragment"/>

    <action
        android:id="@+id/toChat"
        app:destination="@id/chatFragment"/>

</navigation>